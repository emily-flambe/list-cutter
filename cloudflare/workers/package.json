{
  "name": "cutty",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --config wrangler.prod.toml",
    "preview:upload": "wrangler versions upload",
    "preview:list": "wrangler versions list",
    "types": "wrangler types",
    "test": "vitest",
    "test:ci": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest --watch",
    "test:auth": "vitest run tests/auth/",
    "test:performance": "vitest run tests/performance/",
    "test:performance:watch": "vitest --watch tests/performance/",
    "test:benchmark": "node tests/performance/benchmark.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --platform=node --target=es2022 --minify --tree-shaking=true --keep-names --sourcemap=external --legal-comments=none",
    "build:dev": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --platform=neutral --target=es2022 --sourcemap=inline",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.53",
    "@cloudflare/workers-types": "^4.20241230.0",
    "@playwright/test": "^1.48.2",
    "@types/benchmark": "^2.1.5",
    "@types/node": "^22.10.6",
    "@typescript-eslint/eslint-plugin": "^8.20.0",
    "@typescript-eslint/parser": "^8.20.0",
    "artillery": "^2.0.19",
    "benchmark": "^2.1.4",
    "esbuild": "^0.24.2",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "wrangler": "^4.24.3"
  },
  "dependencies": {
    "@vitest/coverage-v8": "2.0.5",
    "drizzle-orm": "^0.38.4",
    "hono": "^4.6.16",
    "jose": "^5.10.0",
    "vitest": "2.0.5",
    "zod": "^3.24.1"
  }
}
